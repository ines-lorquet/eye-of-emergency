import random

class DecisionTree:
    def __init__(self, X, y):
        self.best_num_impurity = []
        self.feature = [] # Doit contenir toute les features
        self.feature = len()

    def select_random_feature(self):
        len(self.feature)

    def gini_impurity(self, right, left):
        return 1 - (right)
    
    def return_shape(self):
        return print(self.X.shape, self.y.shape)
    

# Select Random feature
# Calculate Gini for each feature
